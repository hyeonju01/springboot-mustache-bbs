{{>layouts/header}}
    <div>
        {{# id}}
            <h1>{{id}} {{username}} Hello!</h1>
            <button type="button" class="btn btn-primary">Primary</button>
        {{/id}}
        {{^ id}}
            <h1>{{username}} Hello!</h1>
        {{/id}}
    </div>
{{>layouts/footer}}